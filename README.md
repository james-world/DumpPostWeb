# Description #

This is a very simple ASP.NET Core site created to test Web Hooks. It stores the last 20 requests sent to it in memory, and displays them when you send an HTTP GET request to /show. Favicon requests are not logged.